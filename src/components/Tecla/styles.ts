import styled from 'styled-components'

export const TeclaContainer = styled.button`
  all: unset;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  &:hover {
    filter: brightness(1.2);
  }
`
